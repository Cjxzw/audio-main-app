# 移动端语音 Agent 项目任务文档

> **文档用途**：上下文重置后的完整记忆恢复。本文档记录了从需求调研到架构决策的完整过程，是后续开发的唯一权威参考。
>
> **创建日期**：2026-07-01
> **最后更新**：2026-07-02

---

## 一、项目概述

### 1.1 项目目标

构建一个**移动端独立运行的语音优先 Agent 应用**，具备"秘书级"能力：能聊天、有活能派出去、活干完了能汇报、汇报时机由状态机控制（忙时择机汇报，闲时主动汇报）。

### 1.2 核心需求

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| R1 | 语音对话 | P0 | 语音输入 + 语音输出，支持打断 |
| R2 | Agent 框架本地运行 | P0 | 上下文管理、工具调用解析、状态机全部在手机端执行，不需要后端服务器 |
| R3 | 云端 LLM 调用 | P0 | 手机直接发 HTTP 请求到云端 LLM API（StepFun/OpenAI），不经后端中转 |
| R4 | 工具调用 | P0 | 简单工具：天气查询、搜索等；复杂工作委派给 PC 端 Agent |
| R5 | 异步任务委派 | P1 | 用户语音指令 → Agent 派活给后台 Worker → 不阻塞，继续聊天 |
| R6 | 闲时汇报策略 | P1 | Worker 完成后：用户忙时排队等候，用户闲时主动汇报，紧急任务可打断 |
| R7 | 任务委派到 PC | P2 | 通过 WebSocket/HTTP 将复杂任务发送到 PC 端 Agent 执行 |
| R8 | Android 优先 | P0 | 首要平台 Android（Kotlin），未来可扩展 iOS |

### 1.3 关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 语音模式 | **级联管线**（ASR→LLM→TTS）而非 Realtime | Realtime 模式的优势（低延迟、声纹）在本场景价值有限；级联管线自由度更高，工具调用和结果汇报更可控 |
| Agent 框架 | **混合方案**：LangChain4j（Agent 层）+ 自建语音管线（参考 Pipecat） | LangChain4j 提供成熟的工具调用和上下文管理，省 3000+ 行自研；语音管线是核心差异化，需自己掌控 |
| 不用 Pipecat 后端 | 砍掉 Pipecat 的 Bus/Worker/Transport 层 | 这些是服务端组件，移动端不需要 |
| 不用 Realtime API | 选择级联而非 OpenAI Realtime / StepFun Realtime | 级联管线中 Agent 状态完全在本地，不受服务端会话限制；长文本结果汇报不会污染会话状态 |
| 不自建完整 Pipeline | 参考 Pipecat 的 Frame/Pipeline 设计思想，用 Kotlin 重写 | 逐行翻译 18000 行 Python 成本太高（6-10 周）；保留设计思想用 Kotlin 惯用写法重写，工作量降 65% |

---

## 二、开源调研结论

### 2.1 语音 Agent 框架对比

| 项目 | 定位 | Stars | 语音 | 任务委派 | 主动汇报 | 状态机 | 移动端 |
|------|------|-------|------|----------|----------|--------|--------|
| **Pipecat** | 语音框架 | 13k | ✅ 级联+Realtime | ✅ WorkerBus | ⚠️ 需自行组装 | ✅ Flows+Idle | ⚠️ 仅客户端入口 |
| **LiveKit Agents** | 语音框架 | 3.7k | ✅ 级联 | ✅ TaskGroup | ⚠️ 需自行触发 | ⚠️ TaskGroup | ❌ |
| **TEN Framework** | 语音框架 | 10.8k | ✅ 级联+Realtime | ⚠️ 模块化 | ⚠️ 自定义 | ⚠️ 节点级 | ❌ |
| **GLaDOS** | 语音产品 | 5.6k | ✅ 级联 <600ms | ✅ MCP工具 | ✅ 主动行为 | ✅ 情感+多Agent | ❌ 纯Linux桌面 |
| **Pipecat** | 语音框架 | 13k | ✅ | ✅ | ❌ | ✅ | ❌ 需后端 |

**结论**：开源社区不存在"移动端独立运行 + 语音交互 + LLM 驱动 + 秘书级 Agent 能力"的完整产品。

### 2.2 移动端 Agent 框架对比

| 项目 | 平台 | 语言 | Stars | 语音 | 工具调用 | 状态管理 | Android可用 |
|------|------|------|-------|------|----------|----------|-------------|
| **PhoneClaw** | iOS | Swift | 1,118 | ✅ ASR+TTS+LIVE | ✅ Skill系统 | ✅ Router | ❌ 仅iOS |
| **LangChain4j** | JVM | Java/Kotlin | 12,484 | ❌ | ✅ @Tool | ✅ ChatMemory | ⚠️ 有兼容性坑 |
| **langchain-swift** | iOS | Swift | 归档 | ❌ | ✅ | ✅ | ❌ 仅iOS |
| **Apple Foundation Models** | iOS | Swift | 闭源 | ❌ | ✅ Tool协议 | ✅ Session | ❌ 仅iOS |

**结论**：Android 上没有任何成熟的、原生的、带语音的 Agent 框架。PhoneClaw 是最佳架构参考（但仅 iOS/Swift）。LangChain4j Agent 能力最强但无语音，且在 Android 上有已知的枚举/反射兼容性问题（解法：只用核心模块，避开 agentic 实验性模块和反射重的功能）。

### 2.3 GLaDOS 架构参考价值

GLaDOS 虽然是 Python/Linux 桌面项目，但其架构设计对本项目有重要参考价值：

| GLaDOS 特性 | 参考价值 | 如何借鉴 |
|-------------|----------|----------|
| 双通道调度（Priority Lane vs Autonomy Lane） | **高** | 用户语音输入走优先通道，后台结果汇报走自主通道，互不阻塞 |
| Slot 状态共享 | **高** | 子任务通过 Slot 共享状态（pending/running/completed + importance + notify_user） |
| Cooldown + Coalesce | **高** | 两次自主汇报之间的最小间隔（默认 20s），防止队列堆积 |
| 自主决策循环（self-prompting） | **中** | 每个 tick 周期决定是 speak 还是 do_nothing |
| Society of Mind 多 Agent | **中** | 子 Agent 不直接通信，通过 Slot 间接交互 |
| MCP 工具调用 | **中** | 命名空间隔离 `mcp.<server>.<tool>` |

### 2.4 Pipecat 移植评估

对 Pipecat 源码的精确分析（本地路径 `d:\solo工作区\pipecat`）：

| 维度 | 数据 |
|------|------|
| 总代码量 | ~115,000 行 Python |
| 砍掉的部分（Bus/Worker/Transport/CLI/非OpenAI供应商等） | ~75,000 行 |
| 需要移植的核心 | ~18,000 行 Python → ~21,000 行 Kotlin |
| asyncio → kotlinx.coroutines 映射 | 完全可行，1:1 对应 |
| 最大风险 | FrameProcessor 双队列 + AsyncGenerator→Flow 语义映射 |
| 全量移植预估时间 | 6-8 周（1人） |
| **混合方案自研量** | **~7,000 行 Kotlin** |

### 2.5 端侧组件成熟度

| 组件 | 首选方案 | Android支持 | 离线 | 质量 |
|------|----------|-------------|------|------|
| ASR | **Sherpa-ONNX** (Paraformer/SenseVoice) | 完善（预编译APK，NPU加速） | ✅ | 中文优秀 |
| VAD | **Silero VAD** (ONNX) | 完善 | ✅ | 业界标准 |
| LLM | **云端 API**（StepFun/OpenAI） | N/A | ❌ | 最优 |
| TTS | **Sherpa-ONNX TTS** (Piper/Kokoro) | 完善（预编译APK） | ✅ | Piper轻量，Kokoro音质高 |
| 唤醒词 | OpenWakeWord | 完善 | ✅ | 可自定义 |
| ONNX Runtime | onnxruntime-android AAR | 完善 | ✅ | 与Python版API一致 |

---

## 三、系统架构设计

### 3.1 整体架构

```
┌──────────────────────────────────────────────────────────────┐
│                    Android App (Kotlin)                        │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │           语音管线层（自研，参考 Pipecat 设计）             │ │
│  │                                                          │ │
│  │  AudioRecord → VAD(Silero ONNX)                         │ │
│  │      → ASR(Sherpa-ONNX) → [Frame Bridge]                │ │
│  │  TTS(Sherpa-ONNX) ← [Frame Bridge]                      │ │
│  │      ← AudioTrack                                       │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│                           │ 文本/事件                          │
│  ┌────────────────────────▼─────────────────────────────────┐ │
│  │           Agent 层（LangChain4j）                         │ │
│  │                                                          │ │
│  │  ChatMemory (上下文管理: MessageWindow/TokenWindow)      │ │
│  │  @Tool (工具调用: 天气/搜索/任务委派)                      │ │
│  │  AI Services (LLM 调用: StepFun/OpenAI)                 │ │
│  └────────────────────────┬─────────────────────────────────┘ │
│  ┌────────────────────────▼─────────────────────────────────┐ │
│  │        汇报策略层（自研，参考 GLaDOS）                     │ │
│  │                                                          │ │
│  │  UserIdleDetector → ResultQueue (优先级队列)             │ │
│  │  PendingResultReporter (闲时择机汇报)                    │ │
│  │  TaskDispatcher (委派到PC端Agent)                         │ │
│  │  双通道: Priority(用户) / Autonomy(汇报)                  │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
                          │ WebSocket / HTTP
┌──────────────────────────▼───────────────────────────────────┐
│                PC 端 Agent (Coordinator)                      │
│  Task List + Worker Pool + 多 Agent 协作                      │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 语音管线层设计（参考 Pipecat）

#### Frame 系统（简化版，~15种核心 Frame）

```kotlin
sealed interface Frame {
    val timestamp: Long
}

// 系统帧（高优先级，不可打断）
sealed class SystemFrame : Frame {
    data class StartFrame : SystemFrame()
    data class EndFrame : SystemFrame()
    data class CancelFrame : SystemFrame()
    data class InterruptionFrame : SystemFrame()
    data class UserStartedSpeakingFrame : SystemFrame()
    data class UserStoppedSpeakingFrame : SystemFrame()
    data class BotStartedSpeakingFrame : SystemFrame()
    data class BotStoppedSpeakingFrame : SystemFrame()
}

// 数据帧（普通优先级，可被打断）
sealed class DataFrame : Frame {
    data class InputAudioRawFrame(val audio: ByteArray) : DataFrame()
    data class OutputAudioRawFrame(val audio: ByteArray) : DataFrame()
    data class TextFrame(val text: String) : DataFrame()
    data class LLMTextFrame(val text: String) : DataFrame()
    data class TTSTextFrame(val text: String) : DataFrame()
    data class TranscriptionFrame(val text: String) : DataFrame()
    data class FunctionCallResultFrame(val result: String) : DataFrame()
}
```

#### FrameProcessor 基类

```kotlin
abstract class FrameProcessor {
    private val inputQueue = Channel<Frame>(Channel.UNLIMITED)
    private var next: FrameProcessor? = null
    private var prev: FrameProcessor? = null

    // 子类实现：处理帧
    abstract suspend fun processFrame(frame: Frame, direction: FrameDirection)

    // 推送帧到下游
    suspend fun pushFrame(frame: Frame, direction: FrameDirection = FrameDirection.DOWNSTREAM) {
        if (direction == FrameDirection.DOWNSTREAM) {
            next?.processFrame(frame, direction)
        } else {
            prev?.processFrame(frame, direction)
        }
    }
}

enum class FrameDirection { UPSTREAM, DOWNSTREAM }
```

#### Pipeline 链式管道

```kotlin
class Pipeline(vararg processors: FrameProcessor) {
    init {
        // 链式连接所有 processor
        processors.zipWithNext().forEach { (prev, next) ->
            prev.next = next
            next.prev = prev
        }
    }
}
```

#### 管线流转

```
AudioRecord → VADProcessor → ASRProcessor → LLMProcessor → TTSProcessor → AudioTrack
     ↑                                                          |
     └──────────── InterruptionFrame (用户打断时) ───────────────┘
```

### 3.3 Agent 层设计（LangChain4j）

#### 依赖配置（build.gradle.kts）

```kotlin
dependencies {
    // LangChain4j 核心（只用核心模块，避开实验性模块的兼容性问题）
    implementation("dev.langchain4j:langchain4j:1.0.0")
    implementation("dev.langchain4j:langchain4j-kotlin:1.0.0")
    implementation("dev.langchain4j:langchain4j-open-ai:1.0.0")  // OpenAI兼容API（含StepFun）

    // 协程
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.0")

    // HTTP 客户端
    implementation("com.squareup.okhttp3:okhttp:4.12.0")
    implementation("com.squareup.retrofit2:retrofit:2.11.0")

    // ONNX Runtime（VAD + 可选端侧模型）
    implementation("com.microsoft.onnxruntime:onnxruntime-android:1.18.0")

    // Sherpa-ONNX（ASR + TTS）
    implementation("com.k2fsa.sherpa:sherpa-onnx:1.10.8")
}
```

#### 工具定义示例

```kotlin
object AgentTools {
    @Tool("查询指定城市的天气")
    fun getWeather(@P("城市名称") city: String): String {
        // 调用天气 API
        return "明天$city 晴，最高温度 32°C"
    }

    @Tool("委派后台任务到PC端Agent执行")
    fun dispatchTask(
        @P("任务类型") taskType: String,
        @P("任务描述") description: String
    ): String {
        // 异步提交到 Coordinator，立即返回 task_id
        val taskId = taskDispatcher.submit(taskType, description)
        return """{"task_id":"$taskId","status":"dispatched"}"""
    }
}

// Agent 构建
val agent = AiServices.builder(Assistant::class.java)
    .chatLanguageModel(model)  // OpenAI / StepFun
    .chatMemory(MessageWindowChatMemory.withMaxMessages(20))
    .tools(AgentTools)
    .systemMessage("你是一个语音助手，用简洁的中文回答。收到任务委派后，确认已安排即可。")
    .build()
```

#### LangChain4j 桥接层

```kotlin
class LLMProcessor(
    private val agent: Assistant
) : FrameProcessor() {

    override suspend fun processFrame(frame: Frame, direction: FrameDirection) {
        when (frame) {
            is DataFrame.TranscriptionFrame -> {
                // 用户语音转文字后，调用 LLM
                val response = agent.chat(frame.text)

                // LLM 回复推送到 TTS
                pushFrame(DataFrame.LLMTextFrame(response))
            }
            // ... 处理其他帧
        }
    }
}
```

### 3.4 汇报策略层设计（参考 GLaDOS）

#### 双通道架构

```kotlin
class DualChannelScheduler {
    // Priority Lane: 用户输入优先处理，永不阻塞
    private val priorityChannel = Channel<UserInput>(Channel.UNLIMITED)

    // Autonomy Lane: 后台结果汇报，可被用户输入打断
    private val autonomyChannel = Channel<PendingResult>(Channel.UNLIMITED)

    // 状态
    @Volatile var userSpeaking = false
    @Volatile var botSpeaking = false
    @Volatile var idle = false
}
```

#### 闲时检测 + 择机汇报

```kotlin
class PendingResultReporter(
    private val llmProcessor: LLMProcessor,
    private val ttsProcessor: TTSProcessor
) : FrameProcessor() {

    private val resultQueue = PriorityBlockingQueue<PendingResult>()
    @Volatile private var userIdle = false
    @Volatile private var botSpeaking = false
    @Volatile private var reporting = false
    private var lastReportTime = 0L
    private val cooldownMs = 20_000L  // 最小汇报间隔 20s（参考 GLaDOS）

    // 外部调用：Worker 结果到达时入队
    fun enqueueResult(result: PendingResult) {
        resultQueue.put(result)
        tryDrain()
    }

    // 用户空闲时触发
    fun onUserIdle() {
        userIdle = true
        tryDrain()
    }

    // 用户开始说话时
    fun onUserStartedSpeaking() {
        userIdle = false
    }

    // Bot 开始说话
    fun onBotStartedSpeaking() {
        botSpeaking = true
    }

    // Bot 说完
    fun onBotStoppedSpeaking() {
        botSpeaking = false
        tryDrain()
    }

    private suspend fun tryDrain() {
        if (resultQueue.isEmpty() || reporting || botSpeaking) return
        if (!userIdle) return  // 用户忙，排队等候
        if (System.currentTimeMillis() - lastReportTime < cooldownMs) return

        reporting = true
        while (resultQueue.isNotEmpty() && userIdle && !botSpeaking) {
            val result = resultQueue.poll()

            // 紧急结果：即使打断也汇报
            if (result.priority == Priority.URGENT || userIdle) {
                injectResult(result)
                lastReportTime = System.currentTimeMillis()
                // 等模型说完
                while (botSpeaking) delay(100)
            } else {
                resultQueue.put(result)  // 放回队列
                break
            }
        }
        reporting = false
    }

    private suspend fun injectResult(result: PendingResult) {
        // 将结果注入 LLM 上下文，触发语音汇报
        val reportPrompt = "[系统通知] 后台任务 ${result.taskId} 完成：\n${result.summary}\n请用简洁的语音向用户汇报。"
        pushFrame(DataFrame.TextFrame(reportPrompt), FrameDirection.UPSTREAM)
    }
}

data class PendingResult(
    val taskId: String,
    val summary: String,
    val priority: Priority = Priority.NORMAL,
    val timestamp: Long = System.currentTimeMillis()
)

enum class Priority(val weight: Int) {
    URGENT(0),    // 紧急：可打断
    NORMAL(10),   // 普通：闲时汇报
    LOW(20)       // 低优先级：下次空闲时
}
```

### 3.5 汇报时机的四种场景

| 场景 | 用户状态 | 结果优先级 | 行为 |
|------|----------|-----------|------|
| 闲时普通结果 | 空闲 | NORMAL | 主动汇报，注入 LLM 上下文，生成语音 |
| 忙时普通结果 | 正在说话/听回复 | NORMAL | 排队等候，idle 触发后再汇报 |
| 闲时紧急结果 | 空闲 | URGENT | 立即汇报 |
| 忙时紧急结果 | 正在说话 | URGENT | 打断当前对话，优先汇报 |

### 3.6 任务委派协议

```kotlin
// 手机端 → PC端 任务委派
data class TaskDispatch(
    val task_id: String,
    val worker_id: String?,
    val task_type: String,       // "research", "analysis", "coding" 等
    val prompt: String,          // 初始提示词
    val description: String,     // 任务说明
    val priority: String = "normal",
    val callback_url: String     // 结果回调地址
)

// PC端 → 手机端 结果回传
data class TaskResult(
    val task_id: String,
    val status: String,          // "completed" | "failed" | "progress"
    val summary: String,         // 结果摘要（用于语音汇报）
    val full_report: String?,    // 完整报告（可选，存文件）
    val priority: String = "normal"
)
```

---

## 四、开发环境

### 4.1 已安装环境（验证通过）

| 组件 | 版本 | 路径 |
|------|------|------|
| JDK (Temurin) | 21.0.11 LTS | `C:\jdk21\jdk-21.0.11+10` |
| Android SDK cmdline-tools | 12.0 | `C:\android-sdk\cmdline-tools\latest` |
| Android SDK platform-tools | 37.0.0 | `C:\android-sdk\platform-tools` |
| Android SDK build-tools | 34.0.0 + 35.0.0 | `C:\android-sdk\build-tools` |
| Android Platform | API 34 + API 36 | `C:\android-sdk\platforms` |
| NDK | 27.2.12479018 | `C:\android-sdk\ndk\27.2.12479018` |
| CMake (SDK) | 3.22.1 | `C:\android-sdk\cmake\3.22.1` |
| CMake (系统) | 4.3.0-rc1 | `C:\Program Files\CMake\bin` |
| Gradle 缓存 | ~1GB | `C:\Users\mm\.gradle\` |

### 4.2 环境变量（已永久配置）

```
JAVA_HOME = C:\jdk21\jdk-21.0.11+10
ANDROID_HOME = C:\android-sdk
PATH += %JAVA_HOME%\bin;%ANDROID_HOME%\cmdline-tools\latest\bin;%ANDROID_HOME%\platform-tools
```

### 4.3 网络注意事项

- GitHub 访问较慢，Gradle 使用腾讯镜像（`mirrors.cloud.tencent.com`）
- JDK 下载使用清华镜像（`mirrors.tuna.tsinghua.edu.cn`）
- 项目路径含中文字符，需在 `gradle.properties` 中设置 `android.overridePathCheck=true`

### 4.4 缺少的（按需安装）

| 组件 | 何时需要 | 安装命令 |
|------|----------|----------|
| Android Emulator | 需要模拟器测试时 | `sdkmanager "emulator" "system-images;android-34;google_apis;x86_64"` |
| Android Studio | 需要断点调试/UI预览时 | 从 https://developer.android.com/studio 下载 |

### 4.5 已克隆的 Pipecat 仓库（参考用）

| 仓库 | 本地路径 | Gitea 地址 |
|------|----------|-----------|
| pipecat | `d:\solo工作区\pipecat` | http://192.168.8.2:8418/agent/pipecat.git |
| pipecat-client-android | `d:\solo工作区\pipecat-client-android` | http://192.168.8.2:8418/agent/pipecat-client-android.git |
| pipecat-client-android-transports | `d:\solo工作区\pipecat-client-android-transports` | http://192.168.8.2:8418/agent/pipecat-client-android-transports.git |
| pipecat-examples | `d:\solo工作区\pipecat-examples` | http://192.168.8.2:8418/agent/pipecat-examples.git |

Gitea 账户：agent / 密码：123456789

---

## 五、Pipecat 源码关键文件索引

移植时需要参考的核心文件：

| 模块 | 文件路径 | 行数 | 移植优先级 |
|------|----------|------|-----------|
| Frame 系统 | `src/pipecat/frames/frames.py` | 1,630 | P0 |
| FrameProcessor | `src/pipecat/processors/frame_processor.py` | 854 | P0 |
| Pipeline | `src/pipecat/pipeline/pipeline.py` | ~400 | P0 |
| PipelineWorker | `src/pipecat/pipeline/pipeline_worker.py` | 1,142 | P1（简化版） |
| LLM 上下文聚合 | `src/pipecat/processors/aggregators/llm_context.py` | 436 | P1 |
| LLM 响应聚合 | `src/pipecat/processors/aggregators/llm_response.py` | 68 | P1 |
| 句子聚合 | `src/pipecat/processors/aggregators/sentence.py` | 49 | P1 |
| LLMService 基类 | `src/pipecat/services/llm_service.py` | 1,592 | P1（参考设计） |
| TTSService 基类 | `src/pipecat/services/tts_service.py` | 1,534 | P1（参考设计） |
| STTService 基类 | `src/pipecat/services/stt_service.py` | 904 | P1（参考设计） |
| WebsocketService | `src/pipecat/services/websocket_service.py` | 252 | P2 |
| 工具框架 | `src/pipecat/adapters/schemas/function_schema.py` | 120 | P2 |
| 工具框架 | `src/pipecat/adapters/schemas/tools_schema.py` | 109 | P2 |
| VAD (Silero) | `src/pipecat/audio/vad/silero.py` | 225 | P0 |
| VAD Controller | `src/pipecat/audio/vad/vad_controller.py` | 198 | P0 |
| 轮次管理 | `src/pipecat/turns/user_turn_controller.py` | 325 | P1 |
| 空闲检测 | `src/pipecat/turns/user_idle_controller.py` | 135 | P0 |
| FlowManager | `src/pipecat/flows/manager.py` | ~900 | P2 |
| FlowActions | `src/pipecat/flows/actions.py` | ~200 | P2 |

---

## 六、Python → Kotlin 映射参考

| Python | Kotlin | 说明 |
|--------|--------|------|
| `asyncio.Task` | `kotlinx.coroutines.Job` | 异步任务 |
| `asyncio.Event` | `CompletableDeferred<Unit>` | 事件同步 |
| `asyncio.Queue` | `kotlinx.coroutines.channels.Channel` | 消息队列 |
| `asyncio.PriorityQueue` | `Channel<Frame>` + 手动优先级排序 | 优先级队列 |
| `asyncio.sleep` | `delay()` | 延时 |
| `asyncio.wait_for` | `withTimeout()` | 超时 |
| `asyncio.CancelledError` | `CancellationException` | 取消 |
| `AsyncGenerator[Frame]` | `Flow<Frame>` | 流式生成（注意：Flow 是冷流） |
| `dataclass` | `data class` / `sealed class` | 数据类 |
| `websockets` | OkHttp WebSocket | WebSocket 客户端 |
| `onnxruntime` | `onnxruntime-android` AAR | ONNX 推理 |
| `loguru` | Timber | 日志 |
| `inspect` | `kotlin-reflect` | 反射 |
| `openai` Python SDK | 手写 Retrofit + 流式解析 | LLM API |
| `pydantic` | Kotlin data class | 数据验证 |

---

## 七、开发计划

### 第一期：MVP（2-3 周）-- 能说话能调工具

| 模块 | 参考来源 | 预估代码量 | 难度 |
|------|----------|-----------|------|
| Frame 系统（简化版，~15种） | Pipecat frames.py | ~800行 | 中 |
| FrameProcessor + Pipeline | Pipecat frame_processor.py | ~1,200行 | 高 |
| VAD 集成（Silero ONNX） | Pipecat silero.py | ~400行 | 低 |
| ASR 集成 | Sherpa-ONNX Android | ~400行 | 低 |
| TTS 集成 | Sherpa-ONNX Android | ~400行 | 低 |
| LangChain4j 桥接层 | 自研 | ~600行 | 中 |
| 简单上下文管理 | LangChain4j ChatMemory | ~200行 | 低 |
| 工具框架 | LangChain4j @Tool | ~300行 | 低 |
| Android 音频 I/O | Android AudioRecord/AudioTrack | ~500行 | 中 |
| **合计** | | **~4,800行** | |

**MVP 验收标准**：
- 能通过语音与 Agent 对话
- Agent 能调用天气/搜索等简单工具
- 支持用户打断
- LLM 回复能通过 TTS 朗读

### 第二期：状态机 + 汇报策略（2-3 周）

| 模块 | 参考来源 | 预估代码量 | 难度 |
|------|----------|-----------|------|
| 轮次管理完整版 | Pipecat turns/ | ~1,200行 | 中 |
| UserIdleController | Pipecat idle_controller | ~300行 | 低 |
| 结果队列 + 优先级管理 | 自研 | ~400行 | 中 |
| PendingResultReporter | GLaDOS 双通道 | ~800行 | 中 |
| 紧急打断机制 | 自研 | ~400行 | 中高 |
| TTS 句子聚合 | Pipecat sentence.py | ~600行 | 中 |
| **合计** | | **~3,700行** | |

**第二期验收标准**：
- 异步任务完成后能在用户空闲时主动汇报
- 用户正在说话时不会被打断（紧急除外）
- 多个结果排队逐条汇报
- 汇报间隔有 cooldown 控制

### 第三期：任务委派到 PC（1-2 周）

| 模块 | 参考来源 | 预估代码量 | 难度 |
|------|----------|-----------|------|
| WebSocket 通信层 | 自研 | ~500行 | 中 |
| 任务委派协议 | 自研 | ~300行 | 低 |
| 结果回传处理 | 自研 | ~400行 | 中 |
| PC端 Coordinator 接口 | 自研 | ~300行 | 低 |
| **合计** | | **~1,500行** | |

**第三期验收标准**：
- 能通过语音委派复杂任务到 PC 端
- PC 端执行完成后结果回传到手机
- 手机端择机汇报

### 总计

| 阶段 | 代码量 | 时间 |
|------|--------|------|
| 第一期 MVP | ~4,800行 | 2-3周 |
| 第二期 状态机 | ~3,700行 | 2-3周 |
| 第三期 任务委派 | ~1,500行 | 1-2周 |
| **合计** | **~10,000行** | **5-8周** |

---

## 八、技术约束与风险

### 8.1 技术约束

| 约束 | 说明 | 应对 |
|------|------|------|
| 移动端后台限制 | Android Doze mode 会限制后台进程 | 使用 Foreground Service 保活；汇报依赖推送通知唤醒 |
| 端侧 LLM 算力不足 | 手机端 LLM（1.5-8B）规划能力弱 | LLM 全部走云端 API，端侧只做 ASR/VAD/TTS |
| LangChain4j Android 兼容性 | 已知枚举/反射兼容性坑 | 只用核心模块（ChatModel + Tool + ChatMemory），避开 agentic 实验性模块；ProGuard 排除有问题的枚举类 |
| 项目路径含中文 | Gradle 可能警告 | `gradle.properties` 中设置 `android.overridePathCheck=true` |
| Flow 冷流语义 | Kotlin Flow 每次 collect 会重新执行 | 注意生命周期管理，必要时用 StateFlow/SharedFlow |

### 8.2 关键设计原则

1. **Agent 框架在手机本地** -- 上下文管理、工具调用解析、状态机判断全部在手机端，不经后端
2. **LLM 直接调云端** -- 手机直接发 HTTP 到 StepFun/OpenAI，省掉中间层通信延迟
3. **语音管线自己掌控** -- 参考 Pipecat 的 Frame/Pipeline 设计，用 Kotlin 重写，这是核心差异化
4. **汇报策略参考 GLaDOS** -- 双通道调度 + Slot 状态共享 + Cooldown + Coalesce
5. **简单工具手机端执行** -- 天气/搜索等轻量工具直接在手机端跑
6. **复杂工作委派 PC** -- 调研/分析等重活通过 WebSocket 发送到 PC 端 Agent

---

## 九、用户偏好与工程约定

### 9.1 用户偏好（来自项目记忆）

| 偏好 | 说明 |
|------|------|
| 沟通语言 | 中文 |
| 设计哲学 | 尽量减少工具调用，关键数据常驻内存便于实时决策 |
| 系统架构 | 偏好关键数据结构常驻加载，用于实时决策 |

### 9.2 工程约定（来自项目记忆）

| 约定 | 说明 |
|------|------|
| 任务报告原子性 | 报告内容和状态更新必须封装在单个事务中 |
| Agent 列表 | 包含 agent ID、capabilities、identity、online status，通过工具调用按需加载 |
| 任务列表显示 | 显示所有 in_progress 任务 + 所有 unreported 任务 + 最新 5 条 reported 任务 |
| 任务状态 | pending, in_progress, completed, failed |
| 任务数据结构 | 必须有 `reported` boolean 字段和 `reported_at` timestamp |
| Coordinator 架构 | 常驻加载任务列表摘要 + 按需加载 agent 列表 |
| 中间件 | koa-connect wrapper 曾导致 ctx 泄漏，优先使用原生重写而非 wrapper |

### 9.3 PC 端 Coordinator Agent 架构（已有设计）

这是手机端需要对接的 PC 端系统，其设计约束：

```
Coordinator Agent:
  - 常驻加载任务列表摘要（所有 in_progress + 所有 unreported + 最新5条 reported）
  - 按需通过工具调用加载 agent 列表（agent ID + capabilities + identity + online status）
  - 任务报告原子性：report content + status update 在单个事务中
  - 任务状态：pending → in_progress → completed/failed
  - reported 字段：boolean + reported_at timestamp
```

---

## 十、参考项目链接

### 核心参考

| 项目 | GitHub | 用途 |
|------|--------|------|
| Pipecat | https://github.com/pipecat-ai/pipecat | 语音管线 Frame/Pipeline 设计参考 |
| Pipecat Flows | https://docs.pipecat.ai/guides/features/pipecat-flows | 状态机设计参考 |
| Pipecat Idle 检测 | https://docs.pipecat.ai/guides/fundamentals/detecting-user-idle | 空闲检测参考 |
| GLaDOS | https://github.com/dnhkng/GLaDOS | 双通道调度 + 主动行为 + Slot 状态共享参考 |
| GLaDOS 自主架构文档 | https://github.com/dnhkng/GLaDOS/blob/main/docs/autonomy.md | 双通道设计详解 |
| LangChain4j | https://github.com/langchain4j/langchain4j | Agent 框架（工具调用+上下文管理） |
| LangChain4j Tools 文档 | https://docs.langchain4j.dev/tutorials/tools | @Tool 注解用法 |
| LangChain4j Chat Memory | https://docs.langchain4j.dev/tutorials/chat-memory | 上下文管理 |
| PhoneClaw | https://github.com/kellyvv/PhoneClaw | 移动端语音 Agent 架构参考（iOS/Swift） |

### 端侧组件

| 组件 | GitHub | 用途 |
|------|--------|------|
| Sherpa-ONNX | https://github.com/k2-fsa/sherpa-onnx | ASR + TTS（Android 预编译APK） |
| Silero VAD | https://github.com/snakers4/silero-vad | 语音活动检测 |
| ONNX Runtime Android | https://github.com/microsoft/onnxruntime | ONNX 模型推理 |
| Whisper.cpp | https://github.com/ggml-org/whisper.cpp | 备选 ASR |
| Piper TTS | https://github.com/rhasspy/piper | 备选 TTS |

### LLM API

| 服务 | 用途 | 备注 |
|------|------|------|
| StepFun Step-Audio-2.5-Realtime | 中文语音场景 | WebSocket，兼容 OpenAI Realtime API 格式 |
| StepFun 文本 LLM | 工具调用 | OpenAI 兼容 API |
| OpenAI GPT-4o | 通用 LLM | 工具调用原生支持 |

---

## 十一、下一步行动

重置上下文后，按以下顺序执行：

1. **创建 Android Kotlin 项目骨架** -- Gradle 配置、依赖引入、基础目录结构
2. **实现 Frame 系统** -- 参考 Pipecat frames.py，用 Kotlin sealed class 实现 ~15 种核心 Frame
3. **实现 FrameProcessor + Pipeline** -- 参考本文档第 3.2 节的设计
4. **集成 Sherpa-ONNX ASR/TTS** -- 验证端侧语音能力
5. **集成 Silero VAD** -- 验证语音活动检测
6. **集成 LangChain4j** -- 验证工具调用和上下文管理
7. **验证端到端流程** -- 语音输入 → ASR → LLM(工具调用) → TTS → 语音输出

---

## 十二、学术参考

| 论文/项目 | 机构 | 价值 |
|-----------|------|------|
| ProactiveAgent | 清华大学 THUNLP + 面壁智能 | 主动式 Agent 范式：主动推断→提出任务→等待用户接受→执行 |
| ProAct | 上海交通大学 | 主动决策协议，指出仅有"主动尝试"不够，需方向正确 |

GitHub: https://github.com/thunlp/proactive-agent

---

## 十三、MVP 开发进展与问题诊断（2026-07-02）

### 13.1 已完成功能

第一期 MVP 核心链路已打通：

| 模块 | 状态 | 说明 |
|------|------|------|
| Frame 系统 | ✅ 完成 | SystemFrame/DataFrame 双向链表，fast-path 系统帧传播 |
| FrameProcessor + Pipeline | ✅ 完成 | 6 节点链：AudioIn→VAD→ASR→LLM→TTS→AudioOut |
| VAD（Silero ONNX） | ✅ 完成 | sherpa-onnx 1.13.2 AAR，Silero VAD v4 模型 |
| ASR（Paraformer int8） | ✅ 完成 | 217MB 模型，非流式整段推理 |
| TTS（Piper zh_CN-huayan） | ✅ 完成 | 60MB 模型，同步 generate 分块推送 |
| LLM（LangChain4j + MiMo API） | ✅ 完成 | 云端调用 mimo-v2.5，工具调用框架 |
| Android 前台服务 | ✅ 完成 | FOREGROUND_SERVICE_TYPE_MICROPHONE |
| 文件日志 + 崩溃捕获 | ✅ 完成 | Timber FileLogTree + UncaughtExceptionHandler |
| 两阶段 StartFrame 初始化 | ✅ 完成 | 先加载全部模型再启动录音，修复并发崩溃 |

### 13.2 闪退问题诊断与修复

**现象**：App 启动正常，一说话就闪退。Java UncaughtExceptionHandler 未捕获到异常。

**诊断过程**：

1. **初始假设（并发模型加载）**：StartFrame 传播导致 AudioInput 先启动录音，ASR（217MB）模型加载期间 VAD 已在另一线程推理，并发 ORT Session 创建触发崩溃
2. **修复尝试 1**：两阶段 StartFrame 初始化（先加载模型再启动录音）——Gradle 缓存导致旧代码未生效，误判为无效
3. **修复尝试 2**：替换 ORT .so 为 1.17.1——ABI 不兼容，native 库按 ORT 1.24.x API 编译
4. **关键突破**：通过 `adb logcat -b crash` 抓到确切错误：
   ```
   java.lang.RuntimeException: Vad_acceptWaveform: GetElementType is not implemented
       at com.k2fsa.sherpa.onnx.Vad.acceptWaveform(Native Method)
   ```
5. **确认根因**：禁用 ASR/TTS 后 VAD 跑 786 帧不崩溃（之前 130 帧就崩），确认崩溃与 ASR/TTS 模型并发加载有关
6. **覆盖安装陷阱**：`adb install -r` 覆盖安装不替换旧 dex 文件，必须先 `adb uninstall` 再 `install`

**最终修复**：两阶段 StartFrame 初始化 + 卸载后全新安装

```kotlin
// VoiceAgentService.setupPipeline() 中的关键改动
serviceScope.launch {
    // 阶段1：从 VAD(索引1) 开始传播 StartFrame
    // VAD → ASR → LLM → TTS → AudioOut 同步加载所有模型
    // AudioInput(索引0) 不在传播路径上，不会启动录音
    pipe[1].input(SystemFrame.StartFrame)
    
    // 阶段2：所有模型已加载，启动录音
    pipe[0].input(SystemFrame.StartFrame)
}
```

**验证结果**：VAD 连续运行 427 帧（约 14 秒）无崩溃，语音对话完整跑通。

### 13.3 遗留问题：打断策略过于敏感

**现象**：Bot 播报时，麦克风收到回声/噪声，VAD 误判为用户说话，触发 InterruptionFrame 打断自己的回复。用户只听到一声回复后 bot 就不再响应。

**根因分析**（对照 Pipecat 成熟实现）：

| 问题 | 当前实现 | Pipecat 实现 |
|------|----------|-------------|
| VAD 判定 | 单门控：模型 threshold=0.5 | **双门控**：置信度 ≥ 0.7 **且** 音量 ≥ 0.6 |
| 状态机 | 布尔值 inSpeech | **4 状态去抖**：QUIET→STARTING→SPEAKING→STOPPING |
| 最短语音 | 0.25s（无去抖，一帧触发） | 0.2s 连续帧确认 |
| 音量平滑 | 无（仅调试日志） | 指数平滑 factor=0.2 |
| 回声防护 | 仅靠 Android 系统 AEC | 音频滤波器（Krisp/RNNoise）在 VAD **之前** |

### 13.4 后续计划：移植 Pipecat VAD 模式

直接照搬 Pipecat 的三个核心模式到 VADProcessor：

#### 模式 1：双门控 VAD

```kotlin
// Pipecat 的 VADParams 默认值
val VAD_CONFIDENCE = 0.7f    // 模型置信度阈值
val VAD_MIN_VOLUME = 0.6f    // 音量阈值（EBU R128 归一化）

// 双门控判定
val isSpeech = confidence >= VAD_CONFIDENCE && smoothedVolume >= VAD_MIN_VOLUME
```

#### 模式 2：四状态去抖状态机

```kotlin
enum class VadState { QUIET, STARTING, SPEAKING, STOPPING }

// 状态转移：
// QUIET + 双门控达标 → STARTING (count=1)
// STARTING + 达标 → count++ → count >= startFrames → SPEAKING（确认！）
// STARTING + 不达标 → QUIET（重置）
// SPEAKING + 不达标 → STOPPING (count=1)
// STOPPING + 不达标 → count++ → count >= stopFrames → QUIET（确认！）
// STOPPING + 达标 → SPEAKING（恢复）

// 帧数计算
val startFrames = (VAD_START_SECS / vadFrameDurationSec).roundToInt()  // 0.2s → 6帧
val stopFrames = (VAD_STOP_SECS / vadFrameDurationSec).roundToInt()    // 0.2s → 6帧
```

#### 模式 3：指数音量平滑

```kotlin
// Pipecat 的 _get_smoothed_volume()
private var smoothedVolume = 0.0f
private val SMOOTHING_FACTOR = 0.2f

fun updateVolume(currentVolume: Float) {
    smoothedVolume = smoothedVolume + SMOOTHING_FACTOR * (currentVolume - smoothedVolume)
}
```

#### 实施优先级

| 改动 | 文件 | 预估工作量 | 效果 |
|------|------|-----------|------|
| 双门控（音量预过滤） | VADProcessor.kt | ~30行 | 过滤绝大部分静音/噪声 |
| 四状态去抖 | VADProcessor.kt | ~80行 | 消除短噪声误触发 |
| 指数音量平滑 | VADProcessor.kt | ~15行 | 避免单帧能量跳变 |
| Bot 播报打断保护 | VADProcessor.kt | ~20行 | 回声不触发打断 |

**总工作量**：约 150 行 Kotlin 代码，全部在 VADProcessor 内部，不影响其他模块。

### 13.5 构建与部署备忘

| 事项 | 说明 |
|------|------|
| 编译命令 | `cd 项目目录 && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain assembleDebug --no-build-cache --rerun-tasks` |
| 缓存问题 | Gradle build cache 会静默提供旧编译产物。修改源码后必须删除 `app/build` 目录 + 使用 `--no-build-cache --rerun-tasks` |
| 安装方式 | APK 345MB，WiFi ADB 传输超时，需手动传输安装 |
| 覆盖安装陷阱 | `adb install -r` 可能不替换旧 dex，必须先 `adb uninstall` 再 `install` |
| 文件锁问题 | Gradle 打包时旧 APK 被锁，需杀 java/adb 进程后删除 |
| 无线 ADB | `adb connect 192.168.8.21:端口`（端口在开发者选项→无线调试页面查看，动态分配） |
| 日志读取 | `adb shell run-as com.agent.voiceassistant cat files/voice-agent.log` |

---

## 十四、VAD 修复与管线阻塞问题解决（2026-07-02 下午）

### 14.1 UI 布局调整

| 调整 | 说明 |
|------|------|
| 日志窗口缩小 | 从全屏缩小到固定 120dp，底部可滚动 |
| 新增聊天列表 | RecyclerView 展示用户/助手对话记录（ITEM_CHAT_MESSAGE.xml 左右气泡） |
| 新增语音音量条 | 24 段条形图实时显示录音 RMS（绿→黄→红渐变） |

### 14.2 VAD 三重防护修复

按照计划移植 Pipecat VAD 模式，在 VADProcessor.kt 内部实施：

| 防护层 | 逻辑 | 常量 |
|--------|------|------|
| Bot 播报保护 | botSpeaking=true 跳过 VAD | - |
| 音量预过滤 | RMS < VOLUME_GATE 跳过 | VOLUME_GATE=0.0015f |
| 四状态去抖 | QUIET→STARTING(6帧)→SPEAKING→STOPPING(6帧)→QUIET | START/STOP_FRAMES=6 |

关键日志：`VAD heartbeat: frame#N, state=X, bypassed=N, segments=N, smoothedVol=X.XXXX`

### 14.3 Bot 播报阻塞导致的闪退修复

**现象**：Bot 播报时应用闪退，logcat 无 fatal 日志，但 voice-agent.log 显示 `botSpeaking` 持续 3 分钟未恢复。

**根因**：
- `AudioOutputProcessor` 收到第一帧推 `BotStartedSpeakingFrame`，但正常播报完成后从不推 `BotStoppedSpeakingFrame`
- VAD `botSpeaking` 永远锁定为 true，后续语音检测全部跳过

**修复步骤**：

1. `OutputAudioRawFrame` 增加 `endOfUtterance: Boolean = false`
2. `TTSProcessor` 末帧标记 `endOfUtterance=true`
3. `AudioOutputProcessor`：
   - 收到末帧时计算 `durationMs = totalSamples * 1000 / sampleRate`
   - 启动独立协程 `delay(durationMs + 100ms)` 停止播放
   - 推 `BotStoppedSpeakingFrame`

**二次修复（本节）**：

第二次测试发现仍闪退。日志显示每个句子的 `endOfUtterance` 都在发射，但 `totalFramesWritten` 持续累加导致 `durationMs` 计算错误地大。

| 问题 | 修复 |
|------|------|
| `totalFramesWritten` 跨句子累加 | 用 `utteranceSampleCount` 替代，每轮开始重置 |
| 多个 endOfUtterance 协程并发 | `currentDelayJob?.cancel()` 取消上一轮协程 |
| 首句过短导致 BotStoppedSpeaking | 每句独立计时+协程，不再全局判断 |

**验证**：看到 `AudioOutput: >>> BotStoppedSpeaking EMITTED` 日志，间隔合理。

### 14.4 调试菜单需求

为避免每次改参数都需要重新编译，计划新增二级菜单动态调整关键参数：

| 参数 | 当前值 | 范围 | 调试重要性 |
|------|--------|------|------------|
| VOLUME_GATE | 0.0015 | 0~0.02 | ⭐⭐⭐ |
| START_FRAMES | 6 | 1~10 | ⭐⭐⭐ |
| STOP_FRAMES | 6 | 1~10 | ⭐⭐⭐ |
| VOLUME_SMOOTH_FACTOR | 0.2 | 0.05~0.5 | ⭐⭐ |
| threshold（Silero VAD） | 0.5 | 0.1~0.9 | ⭐⭐ |
| minSpeechDurationSec | 0.25 | 0.1~1.0 | ⭐⭐ |
| maxChars（句子聚合） | 80 | 40~200 | ⭐ |

**实现方案**：新增 `ParameterManager` + `SettingsActivity` + `SeekBar` 实时热更新。

### 14.5 Recent Build/Tools Fixes

| 问题 | 解决方案 |
|------|----------|
| 编译 TRAE 沙箱限制 | `gradle.properties` 添加 `kotlin.compiler.execution.strategy=in-process` |
| 安装旧 dex 覆盖 | 每次改代码必须 `adb uninstall` + `adb install` |
| msvcrt 缺失导致无法启动 | 每次重启后重新安装 |
| 崩溃日志抓取 | `adb logcat -b crash` 或 `run-as com.agent.voiceassistant cat files/voice-agent.log` |

### 14.6 Current Project Status

**已验证可语音对话**：

- [x] 语音输入 语音识别 云端 LLM TTS 语音输出
- [x] Bot 开始/结束说话帧正常触发（无阻塞）
- [x] UI 聊天列表实时更新，音量条正常显示

**待修复/优化**：

- [ ] VOLUME_GATE 仍为硬编码，无法动态调整后实时生效
- [ ] 长句播报尚未完整验证（上次 145707 samples 的多句回复）
- [ ] 手机端 `botSpeaking` 与 PC 端的 Bot 状态同步

**下一步**：实现配置页 → 通过实测调 VOLUME_GATE → 增加 `ParameterManager` 支持动态修改各项参数

---

> **文档结束。重置上下文后，将本文档内容提供给新会话即可恢复全部上下文。**
