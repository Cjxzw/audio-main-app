# 开发日志

最后更新：2026-07-19

本文档用于在没有聊天上下文的情况下恢复项目背景和继续开发。新 Agent 接手时应先阅读本文件，再看 `README.md` 和代码。

## 1. 项目初衷

本项目要做的是一个 Android 端语音优先的随身 Agent。主场景不是手持手机聊天，而是用户佩戴智能音频眼镜或蓝牙耳机，通过语音输入指令，并通过耳机或眼镜听取 Agent 回复。

目标体验是“贴身秘书”：

- 用户无需拿出手机，随时通过耳机/眼镜唤醒。
- Agent 能持续在后台待命。
- 用户可以随口交代事情。
- 简单问题直接回答。
- 复杂任务后续派发给下游 Agent 或后端执行。
- 下游任务完成后，App 能主动向用户汇报。
- 休眠时停止收音以降低耗电，但服务仍保持运行，用于接收后端事件和触发主动汇报。

手机自带麦克风和扬声器只是备选场景。主打场景是系统默认音频路由下的蓝牙耳机、智能音频眼镜等外部设备。

## 2. 最终形态

期望最终 App 具备以下能力：

- 注册为可被系统语音助手入口或音频设备按键唤醒的助手应用。
- 伪装/表现为媒体播放类应用，利用媒体会话和前台服务保持后台可控运行。
- 支持播放键或耳机按键在唤醒和休眠之间切换。
- 唤醒后开始收音，完成一轮对话后再恢复监听。
- TTS 播放期间停止收音，避免把自己的播报录回去。
- 休眠状态不开麦，但保留后台服务和后端连接。
- 支持主动汇报：后端或任务系统推送结果后，App 可临时唤醒并播报。
- 支持会话管理、长期记忆、工具调用和任务委派。
- 支持天气、搜索、日程、提醒、PC 端 Agent 派活等工具能力。

## 3. 当前路线

项目早期尝试过本地 ASR/TTS/VAD 模型，APK 一度超过 300MB，调试和分发成本过高。当前已切换为云端优先路线：

- ASR：MiMo 云端 `mimo-v2.5-asr`
- LLM：MiMo/OpenAI 兼容接口，默认 `mimo-v2.5`
- TTS：MiMo 云端 `mimo-v2.5-tts`
- VAD：App 内轻量 RMS 端点检测，不使用本地大模型

本地 Sherpa/ONNX 相关旧代码和占位类仍在仓库中，但不是当前主路径。`app/build.gradle.kts` 已禁用 assets 打包，旧本地模型也不应进入仓库。

## 4. 当前已实现

### 4.1 最小语音闭环

当前主循环在 `VoiceAgentService` 中：

1. `SimpleVadRecorder` 使用 `AudioRecord` 开始录音。
2. RMS 端点检测判断一句话开始和结束。
3. 录音结束后停止收音。
4. `CloudSpeechClient.transcribe()` 将 WAV 发送给 MiMo ASR。
5. ASR 文本进入 LLM。
6. LLM 以 SSE 分离返回思考、正文和原生工具调用事件。
7. AgentLoop 执行工具并用 `role=tool` 回填，直到获得最终正文。
8. `SpeechSegmenter` 按句切分正文，思考和工具参数不会进入 TTS。
9. 句子进入流式 TTS，播放完成后重新进入监听。

当前 TTS 默认启用流式接口，失败时回退为整句合成。

### 4.2 后台运行和休眠

已实现：

- `ForegroundService`
- 通知栏常驻
- Media3 `MediaLibraryService`/`MediaSession`
- 播放/暂停类媒体控制回调
- 休眠状态 `ServiceState.DORMANT`
- 休眠时停止录音并释放当前播放器
- 息屏后台仍可响应

### 4.3 音频路由

当前原则是优先选择用户已连接的蓝牙耳机、音频眼镜、USB 或有线设备，并在语音会话期间显式建立和核验输入输出路由。

原因：

- 智能眼镜和蓝牙耳机通常已经由硬件厂商做了麦克风阵列、降噪和人声增强。
- 只在 Agent 激活期间使用通信音频路由，休眠时立即释放并恢复系统音频模式。
- 外部设备存在但实际麦克风路由无法建立时，不静默切到错误输入，而是提示失败并返回休眠。

`AudioRouteManager` 负责候选设备选择、`setCommunicationDevice()`/旧版 SCO 兼容、对象级首选设备设置、真实 `routedDevice` 核验和释放。

### 4.4 当前 App 包大小

旧本地模型和 AAR 已从当前构建链路移除，当前 Debug APK 约 12MB。仓库不应提交旧模型、AAR、APK 或 build 产物。

### 4.5 Agent 框架与工具调用

当前主路径已改为主流 Agent Harness 结构：

- 工具使用原生 OpenAI 兼容 `tool_calls` 和 JSON Schema，不再依赖提示词生成 XML。
- 模型工具调用、工具执行结果和最终正文可以进行多轮循环。
- 工具调用带 `tool_call_id`，参数错误会作为工具结果返回给模型修正。
- 连续重复工具批次会被阻止，工具结果进入模型前有长度上限。
- SSE 将 `reasoning_content`、`content`、`tool_calls` 和结束原因分别处理。
- 深度思考默认关闭。每个用户回合最多升级一次，回合结束后立即恢复关闭。
- 深思工具调用会完整回传 `reasoning_content`；一轮结束后不把内部思考写入长期聊天历史。
- 当前原生工具包括本地记忆、定位、天气、网络搜索和内部深思升级开关。
- Hub 工具将在枢卫通讯协议接入后加入 `CONNECTED` 工具 Profile。

### 4.6 流式 TTS 首尾噪声修复

MiMo TTS 的 SSE 除音频事件外，还会返回文本预览、结束状态和用量等 JSON 控制事件。旧逻辑在已成功解析 JSON、但其中没有音频字段时，仍会把整段 JSON 当作 base64 解码；Android 的宽松 base64 解码器会把它变成短随机 PCM，造成播报前后的刺啦声。

当前修复包括：

- JSON SSE 事件只从明确的音频字段提取数据；没有音频字段时直接忽略。
- 仅对符合 base64 字符集的非 JSON 数据启用兼容解码。
- 取消会导致削波的 `1.6x` PCM 固定增益，恢复为 `1.0x`。
- 淡入从零开始、淡出归零结束，避免波形边界跳变。
- 实机覆盖安装验证后，播报首尾刺啦声已消失。

## 5. 当前代码结构

```text
app/src/main/java/com/agent/voiceassistant/
├── App.kt
├── MainActivity.kt
├── AssistEntryActivity.kt
├── service/
│   ├── VoiceAgentService.kt      # Android 生命周期、录音、播放、休眠唤醒
│   ├── EventBus.kt               # UI 和服务之间的状态/日志/音量事件
│   └── TaskDispatcher.kt         # 早期任务派发占位
├── cloud/
│   ├── CloudSpeechClient.kt      # MiMo ASR、LLM、TTS HTTP/SSE 调用
│   ├── SimpleVadRecorder.kt      # 轻量录音和端点检测
│   ├── StreamingSentenceBuffer.kt
│   └── WavUtil.kt
├── audio/
│   ├── AudioRouteManager.kt      # 外部设备选择、通信路由、核验和释放
│   ├── AudioInputProcessor.kt    # 早期管线代码
│   ├── AudioOutputProcessor.kt   # 早期管线代码
│   └── AudioConfig.kt
├── agent/
│   ├── LLMConfig.kt              # BuildConfig 注入的 LLM 配置
│   ├── runtime/                  # AgentLoop、Harness、事件、Skill 索引
│   ├── Assistant.kt              # 早期 LangChain4j 封装
│   ├── AgentFactory.kt
│   └── AgentTools.kt
├── pipeline/                     # 早期 Frame/Pipeline 骨架，目前不是主路径
├── report/                       # 主动汇报模型雏形，目前未接入主循环
├── tools/                        # 工具注册、Android 执行环境和虚拟路径
└── ui/                           # 聊天列表、音量条、消息模型
```

## 6. 关键文件说明

- `VoiceAgentService.kt`
  - 当前最重要文件。
  - 负责 wake/sleep、前台服务、录音、ASR、LLM、TTS、播放；媒体控制协议由 `MainMediaLibraryService` 负责。
  - 当前默认启用流式 TTS，并承载原生工具 AgentLoop。

- `CloudSpeechClient.kt`
  - 所有 MiMo 云端调用集中在这里。
  - ASR 使用 `input_audio.data` 传 base64 WAV。
  - LLM 使用 `/chat/completions` SSE。
  - TTS 同时保留非流式和流式方法。

- `SimpleVadRecorder.kt`
  - 固定 16kHz 单声道 PCM。
  - 使用短帧 RMS、噪声底校准、开始阈值、停止阈值判断一句话。
  - 构建 WAV 后送 ASR。

- `AudioRouteManager.kt`
  - 优先选择蓝牙 SCO、BLE 耳机、USB 或有线耳机等外部通信设备。
  - 为录音与播放对象设置首选设备，并在休眠时释放通信路由、恢复系统音频模式。

- `MainActivity.kt`
  - 展示聊天记录、日志、音量条。
  - 请求麦克风和蓝牙权限。

## 7. 当前已知问题

### 7.1 延迟波动

实机测试中，端到端延迟仍有波动。当前已有关键阶段耗时日志，后续需要积累更多数据并显示 P50/P95：

```text
录音结束 -> ASR 完成 -> LLM 首字 -> 首句生成 -> TTS 返回 -> 播放开始 -> 播放结束
```

### 7.2 Hub 工具尚未接入

当前本地 AgentLoop、持久会话、长期记忆、天气、定位和搜索已经跑通。尚缺的是枢卫 Hub 工具：路由缓存、任务派发、任务状态、详细报告和前台会诊。

### 7.3 流式 TTS 需要专项验证

当前主链路已经默认使用 MiMo 流式 TTS，并在失败时回退整句合成。仍需继续优化：

- 首个音频 chunk 的真实到达时间
- 长句断句和跨句衔接
- 网络波动时的缓冲策略
- 首包延迟和播放队列背压

### 7.4 音量问题

手机扬声器下，App 播放听感比抖音等媒体应用小。已尝试：

- `USAGE_MEDIA`
- `CONTENT_TYPE_SPEECH`
- `AUDIOFOCUS_GAIN_TRANSIENT`
- TTS WAV/PCM 软件增益

为避免削波和爆音，当前 PCM 固定增益为 `1.0x`。App 内 `AudioTrack` 音量为 `1.0`，实际响度仍受蓝牙媒体/SCO 独立系统音量影响。后续如需增强，应使用可配置增益配合软限幅，而不是直接硬放大采样。

### 7.5 第三方通话蓝牙路由待验证

用户怀疑 App 运行但处于休眠时，微信来电接听后可能无法把通话音频路由到音频眼镜。当前休眠快照显示 `MODE_NORMAL`、无通信路由客户端、SCO 未激活且没有活动助手通话，尚无长期占用证据。后续需要对微信接听瞬间的 HFP/SCO 路由进行受控抓取，并与强制停止 App 的基线对比。

## 8. 下一步建议

优先级从高到低：

1. 接入 Hub 通讯协议和 `CONNECTED` 工具 Profile。
2. 接入任务完成事件、主动汇报和原文播报。
3. 完善用户说话、思考、工具执行和播报状态机。
4. 优化流式 TTS 衔接、延迟和播放响度。
5. 完善系统语音助手注册和耳机/眼镜唤醒路径。

## 9. 编译运行

配置 `.env`：

```properties
LLM_API_KEY=你的 Key
LLM_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
LLM_MODEL=mimo-v2.5
```

编译：

```powershell
.\gradlew.bat :app:assembleDebug
```

安装：

```powershell
adb install -r app\build\outputs\apk\debug\app-debug-ort1171.apk
```

查看 App 日志：

```powershell
adb shell run-as com.agent.voiceassistant cat files/agent-runtime/logs/voice-agent.log
```

## 10. 仓库维护规则

应提交：

- Kotlin/Android 源码
- Gradle 配置
- UI 资源
- README
- 开发日志
- API 调研文档

不应提交：

- `.env`
- `local.properties`
- APK
- `app/build/`
- `.gradle/`
- `app/libs/`
- `app/src/main/assets/models/`
- AAR 解压目录
- 本地日志和截图

## 11. Pi 风格 Harness 与 Android 执行环境

本轮将语音 Service 中的模型/工具循环抽离为可测试的 Kotlin Agent Harness：

- `AgentLoop` 只负责模型请求、原生工具调用、`role=tool` 回填、重复调用阻断和最终结果判定。
- `MainAgentHarness` 负责单 Agent 串行回合、运行状态、取消，以及后续语音打断所需的 `steer`/`followUp` 双队列。
- 统一事件包含 Agent、Turn、Message、Content、Reasoning、Tool 的开始、增量、完成和失败节点；当前先落盘诊断，后续可直接驱动 UI 和 Hub 观测。
- `VoiceAgentService` 继续只承担录音、Android 生命周期、TTS 和 UI 适配，不再拥有工具循环算法。

新增 `AndroidExecutionEnv`：

- `/source`：Gradle 构建时生成的源码快照，明确排除 `.env`、`local.properties`、BuildConfig 产物和构建目录。
- `/logs`：App 日志不再启动即清空，改为 512 KiB、最多四份的轮转日志。
- `/workspace`：Agent 唯一可通过 `write` 修改的目录。
- `/skills`：Skill 安装目录。
- `read`、`write`、`exec`、`http_request` 已加入原生工具表。
- `exec` 在 App UID 沙箱内拥有较大自由度，但有工作目录、输出上限、最长 120 秒和取消清理；它不是额外的安全沙箱。
- HTTP 凭据以 profile 引用，值由 Android Keystore 加密，模型上下文不接触认证值。

Skill 采用渐进披露：系统提示只注入名称、描述和 `SKILL.md` 路径，真正命中任务后才通过 `read` 加载全文。调试构建附带 Gitea Skill，Release 默认不打包。

验证：

- `./gradlew testDebugUnitTest assembleDebug assembleRelease lintDebug` 通过。
- APK 中确认存在源码快照和 Debug Gitea Skill。
- 源码快照共 77 个文件，静态扫描未发现 API Key 形式的密钥内容。
- 修复旧 ProGuard 工具注解规则和 OSGi 可选类告警，Release 的 R8 压缩链路恢复可用。
- 修复项目原有的 5 个 Android Lint 错误；当前 Lint 无阻断错误。

## 12. 工具链实机反馈修复

首轮实机诊断暴露了三个工具链问题：短 HTTP 响应被误判为 EOF、虚拟目录不能直接读取，以及最后一轮工具执行后没有预算生成总结。

本轮调整：

- HTTP 响应改为循环读取“最多 512 KiB”，短响应和空响应正常结束，超长响应标记截断。
- `read` 遇到目录时返回目录项；新增 `tail_lines`，读取 `/logs` 文件且未指定 offset 时默认从末尾读取。
- `exec` 新增虚拟 `cwd`，模型可在 `/source`、`/logs`、`/workspace`、`/skills` 中直接执行相对命令。
- `ToolResult` 增加成功状态，HTTP 非 2xx/3xx、Shell 非零退出码、参数错误和执行异常都会计为失败。
- AgentLoop 的限制从“模型调用次数”改为“工具轮次”；工具预算之外始终保留一次不带工具的最终总结调用。
- 连续两次工具失败会提前停止重试并进入总结。
- 无法完成最终总结的异常回合会写入一条用户可读的助手兜底消息，保证下一轮上下文知道上轮没有完成。
- 移除每约 100ms 一条的音量 UI 文件日志，仅保留录音和 VAD 的关键节点。

## 13. 语音时延、上下文和单回合思考协议

本轮以手机 APK 内置源码快照为运行时基线，同步到本地后完成以下调整。

### 13.1 统一 5 秒首响应时限

- 当前实现按链路分别设置超时：LLM 首事件 15 秒，TTS 首音频 5 秒，天气和通用 HTTP 请求 5 秒，Exa 网络搜索 8 秒。
- 可重试的网络超时会立即取消并重试一次；重试仍失败后结束当前操作并提示用户检查网络。
- LLM 流式完成和工具完整执行仍允许继续，首事件超时只限制首次有效返回，不限制总生成时间。
- 移除长回复后再次调用 LLM 改写播报文本的额外网络请求，改为本地播报策略。

### 13.2 KV 缓存友好的上下文顺序

固定系统提示词不再包含当前时间、输入间隔或本回合思考策略。请求按稳定程度排列：

1. 固定系统提示词和固定工具案例。
2. API 顶层的稳定工具 Schema。
3. 虚拟文件系统、Skill 索引、凭据 profile 和长期记忆。
4. 当前会话的完整历史消息。
5. 当前时间、输入来源、WiFi/蜂窝网络、ASR 注意事项和本回合引导词。
6. 用户原始输入。

当前用户消息以结构化区块附带动态元数据和引导词。用户原文单独原样保存；动态引导词不进入历史。日志增加系统提示词哈希、工具定义哈希、消息数和字符数，便于分析 KV 缓存和首字延迟。

### 13.3 单回合深度思考

- 每个用户回合都从 `thinking=disabled` 开始。
- 模型根据当前用户消息附带的引导词决定是否调用 `request_deep_reasoning`。
- 该控制工具只在当前回合有效，最终回答产生后立即失效；下一回合不会携带 reasoning、思考申请或申请结果。
- 模型可以只申请思考来提高回答质量，也可以在同一批次返回思考申请和多个参数互不依赖的普通工具调用。
- Agent 收到同批调用后立即把本回合切换为 `thinking=enabled`。网络搜索、本地读取和代码图谱查询等只读工具并行执行；写文件、记忆创建、命令执行等有副作用的工具保持顺序执行。
- 所有工具结果按原 `tool_call_id` 回填，下一次 LLM 请求以深度思考模式处理结果并输出无 Markdown、适合语音播报的简短总结。

### 13.4 历史和持久化边界

- 当前会话不再限制最近 12 条上下文，也不再删除超过 300 条的旧消息或超过 20 个的旧会话。
- 普通工具调用和结果以结构化消息持久化，后续可完整重建工具链。
- reasoning 文本、`request_deep_reasoning` 及其成功返回只存在于当前 AgentLoop 工作上下文，不写入后续 LLM 历史。
- MiMo 提供 1M 上下文窗口，当前版本暂不实现自动摘要或上下文压缩；先通过日志观察上下文长度和首字延迟。

### 13.5 构建与资产检查

- `testDebugUnitTest` 和 `assembleDebug` 通过，新增测试覆盖静态提示词、本回合引导、思考模式切换和同批工具并行。
- Debug APK 约 12.7 MB，包含 `assets/source/` 下的源码快照。
- 当前 APK 已包含与源码匹配的 `assets/codegraph/graph.json.gz` 和报告文件，代码图谱工具可在运行时查询。
- 2026-07-19 已使用无线 ADB 配对端口 `192.168.8.24:45727`、连接端口 `192.168.8.24:46049` 完成全新卸载安装并启动验证，安装时清除了旧会话、记忆和日志。

## 14. 思考反馈与 MiMo TTS 音色统一

- 主动申请深度思考和第三次工具调用自动升级共用同一套等待反馈机制。
- 反馈音与深度 LLM 请求并行，最终正文等待反馈音播放完成后再播，避免音频重叠。
- 反馈音改为 APK 内置 WAV，随机选择且避免连续重复，不再临时请求云端 TTS。
- 所有现场 TTS 和预存反馈音显式使用 MiMo 预设音色 `冰糖`。
- 流式 TTS 显式使用 `pcm16`，非流式回退使用 `wav`。
- `嗯`、`呃`、`这个`等短句分别使用独立的语气、停顿、音高和语速提示生成。
- 生成脚本：`scripts/generate-thinking-audio.sh`。切换预设音色后必须重新生成反馈音资源。

## 15. 工具协议、批量读取和正文防护

- 仅允许 API 原生 `tool_calls` 执行工具。正文中的 XML、JSON、`<tool_call>`、`<function>` 和其他伪协议不再兼容执行，会返回“未受支持的工具调用格式”要求模型重试。
- 已知的 App 物理目录路径会安全映射回 `/source`、`/logs`、`/workspace`、`/skills` 虚拟路径；未知路径仍被拒绝，目录边界未放宽。
- `read` 支持 `paths` 数组，最多 10 项；同一批次的多文件读取会合并为一个工具调用，逐项返回成功或失败。
- 并行工具失败按工具轮次计数，而不是按同批工具数量计数，避免三项并行参数错误立即耗尽失败预算。
- 无工具总结阶段如果再次返回协议、裸 XML/JSON 或纯代码块，最多要求模型修正一次；持续违规时使用本地安全正文，违规内容不会写入历史或进入 TTS。
- Markdown 三反引号中的代码或结构化详情只显示在聊天窗口，不进入 TTS；混合正文播报结束时追加“该回复中有详细信息，请查看手机”。纯代码块会要求模型补充无 Markdown 的口语摘要。
- 流式 TTS 提取器会实时屏蔽 Markdown 代码块及独立的伪 XML 工具标签，避免错误格式在完整响应返回前被播报。

验证：

- `./gradlew testDebugUnitTest assembleDebug` 通过。
- 新增覆盖虚拟物理路径映射、并行批量失败、协议防火墙、Markdown 详情屏蔽、伪 XML 流式屏蔽和最终总结重试的单元测试。

## 16. 标准媒体入口、冷启动注册与通知合并

本轮围绕智能手表、眼镜、蓝牙耳机和手机控制面板的媒体控制链路完成整改：

- 新增标准 `MediaLibraryService`/Media3 会话，启动 App 后立即注册媒体入口，无需先手动唤醒 Agent。
- 本阶段曾增加显式媒体键广播接收器；后续纯 Media3 迁移验证标准媒体服务可接管控制后已删除，避免双入口解释同一按键。
- 补齐 Media3 `prepare` 命令处理与命令白名单，修复部分手表控制器发送 `prepare` 时导致的闪退。
- 保留 Android 语音助手 Activity 入口作为兼容层；媒体入口仍是跨厂商设备的主要唤醒方式。
- App 启动后将休眠态 `VoiceAgentService` 提升为前台服务，保证媒体键和外部控制器能够发现并控制会话。
- 本阶段当时仍保留自管理 Telecom PhoneAccount；该兼容路径已在后续纯媒体架构迁移中彻底删除。
- 将前台服务状态通知与 Media3 媒体卡片合并为同一个 `MediaStyle` 通知。控制面板保留唤醒/休眠按钮，通知栏不再额外显示“休眠中，等待唤醒”的普通通知。

实机验证：

- `connectedDebugAndroidTest` 共 4 项全部通过，覆盖外部 `prepare`、不支持命令过滤、休眠态持久媒体通知和单通知约束。
- Nubia NX769J（Android 14）全新安装后冷启动，只产生一个 `id=41` 的 MediaStyle 通知。
- 未手动启动 Agent 时，物理 `KEYCODE_MEDIA_PLAY_PAUSE` 可完成唤醒，再次发送可完成休眠。
- 休眠状态下确认媒体卡片显示“唤醒”，唤醒状态下显示“休眠”，切换过程无崩溃。

## 17. 可扩展设置中心、模型解耦与个性化播报

本轮建立可持续扩展的设置结构，并把聊天模型与 MiMo 语音能力从运行时拆开。

### 17.1 多级设置中心

- 主聊天页顶栏右侧增加标准齿轮入口，不占用语音和文本输入区域。
- 一级设置只展示“模型与供应商”“语音与播报”等稳定分类；供应商列表、供应商编辑和后续高级选项继续下沉到二级、三级页面。

- 支持多个聊天模型供应商、当前供应商选择、新增/编辑/删除、MiMo 与通用 OpenAI Chat Completions 协议模式，以及保存前连接测试。
- Base URL 明确允许可信局域网 HTTP，但会显示未加密警告；API Key 由 Android Keystore 的 AES/GCM 密钥加密保存。
- 聊天模型配置按用户回合创建快照，设置变更从下一回合生效，不修改正在执行的请求。
- TTS 增益支持 50%–150%、每档 5%，播放 PCM 流和完整 WAV 时统一读取当前设置，避免两套音量逻辑。

### 17.2 模型与语音客户端解耦

- `LlmClient` 独立负责聊天补全和原生工具调用；`CloudSpeechClient` 保留 MiMo ASR/TTS。
- 切换聊天模型不会改变 ASR/TTS 的模型、Key 或 Base URL，避免通用 OpenAI 供应商误接收到语音请求。
- 每回合结束显式关闭聊天客户端的请求、线程池和连接池，防止长时间运行积累空闲网络资源。

### 17.3 终止型个性化 TTS

- 新增 `voice_reply` 原生工具。它只能单独调用，成功后立即结束当前 Agent 回合，不再让模型生成二次总结。
- 模型必须把普通正文留空，并将完整最终回复写入 `text`。App 检测到有效调用后忽略同时返回的普通正文，不显示工具卡，只显示一条淡紫色助手气泡。
- `preset` 模式使用 `mimo-v2.5-tts`，支持冰糖、茉莉、苏打、白桦，以及 speech/singing；唱歌标记只注入 TTS 请求，不进入聊天记录。
- 预设模式将完整文本一次送入流式 PCM16 管线，不经过普通回复的切句器。
- `design` 模式使用 `mimo-v2.5-tts-voicedesign`：`user` 消息为音色描述，`assistant` 消息为完整待播文本，`audio.optimize_text_preview=false`，不传 `audio.voice`。当前等待完整 WAV 后播放。
- 参数无效时不会误播，错误作为 `role=tool` 返回模型修正；终止工具和普通工具混合调用时整批驳回。
- 对唱歌、换音色、特殊语气等明确意图，本回合禁用普通正文的直接 TTS，防止模型违规同时输出正文时提前播报。

验证：

- `./gradlew testDebugUnitTest lintDebug assembleDebug` 通过。
- 新增测试覆盖 MiMo/通用 OpenAI 请求字段、个性化播报参数校验、VoiceDesign 请求字段、终止型工具直接结束回合及混合调用驳回。
- 首轮实机进入“模型与供应商”时暴露 AndroidX Preference 挂载顺序错误：未挂载的 `PreferenceCategory` 提前添加子项会触发 `PreferenceManager.getNextId()` 空指针。现改为先把分类加入 `PreferenceScreen`，再逐项添加供应商。
- 新增设置页仪器回归，并在 NX769J（Android 14）上与媒体会话测试合计运行 5 项，全部通过。

## 18. 去 Telecom 化与纯媒体语音会话

- 删除 `MANAGE_OWN_CALLS`、PhoneAccount、虚拟拨号和 `ConnectionService`，系统通话生命周期不再能够让 Agent 自动休眠。
- 删除旧 `MediaSessionCompat` 和显式媒体键广播接收器，`MainMediaLibraryService`/Media3 成为唯一媒体控制入口。
- `PLAY` 只负责幂等唤醒，`PAUSE`/`STOP` 只负责幂等休眠；设备的播放/暂停切换由 Media3 根据当前播放状态解释。
- 后台休眠使用 `mediaPlayback` 前台服务类型，激活录音时升级为 `mediaPlayback|microphone`；Android 14+ 拒绝后台麦克风时明确降级到通知点击激活。
- 蓝牙麦克风不再依赖通话身份，改由 `MODE_IN_COMMUNICATION`、`setCommunicationDevice()` 和旧系统 SCO 兼容逻辑建立。
- 路由建立采用最多 1.8 秒主动等待，`AudioRecord` 启动后继续核验真实输入路由；外部麦克风路由失败时播放错误音并返回休眠。
- VAD 限制初始噪声校准上限，并根据语音峰值动态调整结束阈值，避免唤醒后立即说话或蓝牙底噪导致录音无法结束。
- 10 秒无有效语音时播放 APK 内置 WAV“即将休眠”；提示结束后监听到第 15 秒总截止点，仍无有效语音则直接进入休眠并释放麦克风、通信设备和音频模式。该提示不发起云端 TTS 请求。
- LLM 超过 3 秒未返回首事件时播放本地等待音；超时和普通网络 I/O 错误均播放本地错误音并在通知、聊天中显示状态。
- WAV 提示和完整 WAV 播报优先使用可显式路由的 `AudioTrack`，避免 `MediaPlayer.setPreferredDevice()` 在蓝牙 SCO 上失败。

## 19. 语义休眠与蓝牙按键边界

- 新增终止型原生工具 `agent_sleep`。用户当前明确表达“退下吧”“没事了”“休眠”“你走吧”“再见”“滚蛋”等结束交互意图时，模型必须单独调用该工具，正文保持为空。
- 工具成功后直接进入现有休眠状态机，停止录音、更新通知和 Media3 状态、播放休眠提示，并释放通信设备与音频模式；当前 Agent 回合自然结束，避免取消自身协程造成半完成状态。
- 提示词同时限制误触发：讨论、引用或询问休眠词语时不得调用。
- NX769J 实机日志确认，Vidda G11 在休眠态可通过 AVRCP `PLAY` 唤醒；启用 SCO 麦克风后，同一物理键被蓝牙栈解释为 HFP `HANGUP_CALL`，不会进入 Media3 `PAUSE`。华为手表直接连接 Media3 控制器，因此播放与暂停均正常。
- 该现象来自经典蓝牙 HFP/SCO 的按键语义切换，不代表 App 恢复了 Telecom 身份。纯媒体 App 若要在 SCO 期间稳定接收眼镜按键，需要厂商 BLE/HID/GATT 控制通道；改用手机麦克风并保持 A2DP/AVRCP 可避免该冲突。
